# Copy to local.env (gitignored) for non-interactive Docker Hub pushes.
# Use a Docker Hub access token, not your password:
#   https://hub.docker.com/settings/security
DOCKER_USER=flexoza
DOCKER_TOKEN=dckr_pat_replace_me
