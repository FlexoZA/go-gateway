# --- Secrets / local env (NEVER commit) ---
.env
.env.local
*.local.env
local.env
deploy/.env
admin/.env
admin/.env.local
# keep the templates
!**/.env.example
!example.env

# --- Generated per-unit stacks (scripts/provision-server.sh) ---
# Keeps the canonical multi-unit deploy/docker-compose.yml; ignores the
# operator-generated single-unit ones (docker-compose.<unit>.yml).
deploy/docker-compose.*.yml

# --- Go build output ---
/bin/
/out/
/gateway
*.test

# --- Node / Next.js (admin app) ---
node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# --- Tooling / editor / OS ---
.claude/
.idea/
.vscode/
*.swp
.DS_Store
